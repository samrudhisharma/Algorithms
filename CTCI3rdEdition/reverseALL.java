/*
static Node reverseLL(Node head)
{
	if(head == null)
	{
		
	}
	else
	{
		Node current  = head;
		Node prev = null;
		
		while(current.next!=null)
		{
			Node temp = currrent.next;
			current.next = null;
			current.next = prev;
			prev = current;
			current = temp;
		}
	}
}

*/